import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const HeaderSearchWithScanner = () => {
  return (
    <View>
      <Text>HeaderSearchWithScanner</Text>
    </View>
  );
};

export default HeaderSearchWithScanner;

const styles = StyleSheet.create({});
